# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr

# Test artifacts
coverage
playwright-report
test-results
playwright/.cache

# Environment
.env
.env.*
!.env.example

# Local files
*.local

# Agent instructions: kept on disk, not published
CLAUDE.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Untouched sources: full-resolution images and the original goal clips.
# Not published anywhere — keep a copy off this machine before deleting it.
_originals

# Content intake: the folders and their instructions are tracked, the media is not
content/incoming/**/*
!content/incoming/**/
!content/incoming/**/README.md
!content/incoming/**/.gitkeep

# Local pipeline artifacts (regenerated on demand)
.cache
data/goals
